{
  "name": "og-website",
  "version": "1.0.0",
  "description": "Web site for Obregon General",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "browser-sync start --server --files '*.html, *.js, *.css'",
    "build:sass": "node-sass -r sass/ -o css/",
    "watch:sass": "npm run build:sass && npm run build:sass -- -w"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/revmc/og-website.git"
  },
  "author": "shaque",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/revmc/og-website/issues"
  },
  "homepage": "https://github.com/revmc/og-website#readme",
  "devDependencies": {
    "browser-sync": "^2.18.6",
    "node-sass": "^4.2.0"
  }
}
